let express,cors;_5c9‍.w("express",[["default",["express"],function(v){express=v}]]);_5c9‍.w("cors",[["default",["cors"],function(v){cors=v}]]);


const server = express();
const enableCors = cors({ origin: 'http://localhost:3000' })

server.get('/status', (_, response) => {
  response.send({
    status: 'Okay',
  })
})

server.post(options('/authenticate', enableCors), '/authenticate', enableCors, express.json(), (request, response) => {
  _5c9‍.g.console.log(
    'E-mail', request.body.email,
    'Senha', request.body.password
  );
  response.send();
})

const PORT = process.env.PORT ? parseInt(process.env.PORT) : 8000;
const HOSTNAME = process.env.HOSTNAME || '127.0.0.1';

server.listen(PORT, HOSTNAME, () => {
  console.log(`Server is listening at http://${HOSTNAME}:${PORT}.`);
})